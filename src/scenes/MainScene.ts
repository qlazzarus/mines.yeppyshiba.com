import { Scene } from '@/enums';

export default class MainScene extends Phaser.Scene {
    constructor() {
        super(Scene.MAIN_SCENE);
    }

    public create(): void {
    }

    public update(): void {
    }
}